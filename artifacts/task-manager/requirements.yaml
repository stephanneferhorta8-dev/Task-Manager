packages:
  "framer-motion": "For smooth list and page transitions adding a premium feel"
  "date-fns": "For elegant date formatting"
  "clsx": "Utility for constructing className strings conditionally"
  "tailwind-merge": "Utility to merge tailwind classes without style conflicts"
  "react-hook-form": "For robust form state management"
  "@hookform/resolvers": "For zod validation integration with react-hook-form"
  "zod": "For client-side schema validation"

notes:
  - Assuming API hooks from `@workspace/api-client-react` return typed data and mutations expect `{ data: ... }` format as generated by Orval.
